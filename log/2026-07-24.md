# 2026-07-24

- Redesigned a Linux permission model for a device-management CLI tool, moving from group-ownership changes to POSIX ACLs to avoid silently stripping unrelated access on production hosts; also tracked down and fixed a container build bug where filesystem ACLs weren't persisting across image layers, and validated the fix by exec'ing into a running test container.
