# 2026-08-25

- Added filtered log-viewing support for in-process service plugins to an internal CLI tool, and replaced a buffered subprocess read with a streaming implementation to remove a memory ceiling on large logs.
- Generalized a database ORM adapter to support multiple database engines, fixed a race condition in a distributed lock-release mechanism using a fencing token, and validated the fix against real database containers with a full regression suite.
