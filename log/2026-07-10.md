# 2026-07-10

- Debugged and fixed a UI/CLI feature flag that was being silently dropped due to a parameter-passing and argument-parsing bug, hardened the CLI's flag validation, and refactored a zip-extraction helper; verified the fix against a live test device.
- Wrote documentation for a newly added end-to-end test suite and flagged a gap in its CI integration compared to a sibling suite, and diagnosed an intermittent local network timeout during service startup as an environment race condition rather than a bug.
