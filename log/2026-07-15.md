# 2026-07-15

- Added a new headless provisioning path to a device management flow, removing a browser-automation dependency from that flow. Separately debugged and fixed a native binary crash in a legacy browser container caused by a missing bundled dynamic linker, resolved by making the launch wrapper invoke the bundled loader explicitly.
