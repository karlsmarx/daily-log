# 2026-07-09

- Added a headless auth path to a device-provisioning CLI to remove a browser-based approval step, fixed a regression from a merge that silently dropped a fleet-detection check, and identified that a newly extracted library mostly duplicated already-existing, more current code.
- Added audit logging to a provisioning API so ignored or changed requests are recorded and surfaced to callers instead of being silently dropped, verified with a full test suite at 100% coverage, and confirmed there was no redundant build work between two related build paths.
- Reviewed the security model of a new internal provisioning API (secret handling, rate limiting, missing audit trail) and discussed mass-rollout scenarios, concluding that a shared-secret transport step should eventually be replaced with per-device public-key authentication.
