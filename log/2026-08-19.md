# 2026-08-19

- Ran a data-safety investigation into a single-writer SQLite architecture, empirically comparing write-ahead-log vs non-WAL throughput and restart-overlap behavior under concurrent load, and produced a written risk evaluation with one remaining open item requiring a multi-node reproduction.
- Investigated migrating a data-access layer off a TypeORM-based ORM onto an internal entity-mapping library, correcting earlier wrong assumptions about the library's real API and versioning, and produced a scoped rollout plan for future call-site conversions plus a proof-of-concept blocked on a package-registry install issue.
- Diagnosed and fixed a post-rebase compile error caused by a leftover variable reference after a squash, then verified other failing tests were pre-existing environment-specific issues unrelated to the change.
- Fixed a CI pipeline failure caused by a missing polyfill import in a test file, and separately fixed several environment-dependent unit tests that made incorrect assumptions about the absence of a system service supervisor.
