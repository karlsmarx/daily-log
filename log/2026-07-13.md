# 2026-07-13

- Debugged a flaky end-to-end test suite where a stale Kubernetes test pod caused false failures, then broadened a test's failure-skip logic to correctly handle connection timeouts instead of only one specific error response. Also fixed a test runner config so an environment-variable override loaded consistently regardless of which command line entry point was used to invoke the suite.
